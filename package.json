{
	"name": "courtlistener-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "cargo install worker-build && wrangler deploy",
		"dev": "cargo install worker-build && wrangler dev --remote"
	},
	"devDependencies": {
		"wrangler": "^7"
	}
}